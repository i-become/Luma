<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luma.system.mapper.SysMenuMapper">

    <select id="selectBaseListByRoleId" resultType="com.luma.system.domain.vo.SysMenuBaseListResp" >
        select m.id, m.parent_id, m.sort, m.title, m.hidden, m.perms from sys_menu m
        inner join sys_role_menu rm on m.id = rm.menu_id
        where rm.role_id = #{roldId}
        order by m.sort
    </select>

</mapper>
